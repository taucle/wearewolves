To run the frontend code, run the following commands:
```sh
pnpm install
pnpm --filter hub dev
```

To create a new app, run the following command from root directory:
```sh
pnpm create vite apps/your-app --template react-ts
```